// Top-level build file where configuration options common to all sub-projects/modules are added.
plugins {
    alias(libs.plugins.android.application) apply false
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
